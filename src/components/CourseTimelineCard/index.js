// Write your code here
import './index.css'

const CourseTimelineCard = props => {
  const {timelineItemsList} = props
  const {categoryId, title, description, duration, tagsList} = props
  return <div></div>
}

export default CourseTimelineCard
